# 引継ぎ

セッションをまたいで作業を継続するための文書です。役割は**「次にどの branch を見ればいいか」の
ポインタ**に限ります。branch 固有の詳細（何が途中か、次に何をするか）はその branch 自身の
コミットや開いてある PR の本文に書き、ここには短いポインタだけを置いてください。恒久的な
リポジトリのルールもここには書きません（`AGENTS.md`・`docs/decisions.md` を参照）。

同時に複数 branch が進行中の場合は、「次にやること」に branch ごと1エントリで列挙してください。
この雛形は複数 branch の並行開発を想定した状態管理は持っていません。本格的に必要になったら
`docs/decisions.md`「3-b. セッション間の引継ぎ」を読んでから設計し直してください。

- `AGENTS.md` がこのファイルを `@` で import しているため、セッション開始時に自動で読み込まれます
- 未記録の変更が残っていると、`.claude/hooks/handoff-check.sh` が1セッションに1回だけ更新を求めます
- セッション終了時、`.claude/hooks/handoff-stamp.sh` が末尾の状態を機械的に**上書き**します（追記ではない）
- 作業開始時は `/checkin`、区切りでは `/handoff`、終了時は `/checkout` を実行してください

**このファイル（引継ぎ文）を更新したら、必ず `main` へマージしてください。** 次のセッションは
`main` を新規クローンし、この docs/handoff.md しか自動では読みません。実際のコード変更は、
未完了なら push 済みの branch/worktree に残したままで構いません（push していれば origin に
残るため消えません）。

---

## いま何をしているか

**顧客管理システム（ひとりで使う CRM）を作っている。全体計画 `docs/plan.json` は当初30項目
＋追記1項目。AI 側だけで進められる項目はすべて終わった。**

**残っているのは `T027`〜`T030` の4項目だけで、どれもユーザー自身の手元でしか実行できない**
（Cloudflare のアカウントへのログインが要る）。手順は `docs/deploy-steps.md` に書いてある。
**ユーザーは「手順を教えてほしい」と答えており、次の一手は手順書に沿った伴走。**

いまの状態: 台帳（登録・一覧・検索・詳細・編集・削除・ごみ箱・五十音順）、やり取りの履歴
（追加・編集・削除・新しい順）、案件（作成・段階の変更・段階ごとの一覧・金額と時期）、
書き出しと取り込み、全ページ共通の枠とメニュー、スマホ幅、何も無いときの案内。
`pnpm run db:setup` のあと `pnpm run dev` で http://localhost:8787 から使える。
**ネット上にはまだ置いていない（`T027`）。本物の顧客データはまだ1件も入っていない。**

`pnpm run plan:progress` を実行すれば、いつでも今の数字が出る（いま 30件中26件＋追記1件）。

## 完了したこと

**`T001`〜`T026` と追記の `T031` を実装し、すべて `main` にマージ済み。**
`automation` が `ci` の項目は `completion-checker` が `verify` の手順を実ブラウザで
なぞって「通った」と判定したものだけを `done` にした。`automation` が `human` の
3項目（`T021` `T022` `T023`）は、実際の画面の写真をユーザーに見せて判断をもらった。

| 項目           | 何ができるか                             | PR      |
| -------------- | ---------------------------------------- | ------- |
| `T001`〜`T014` | 台帳とやり取りの履歴（前セッションまで） | #4〜#18 |
| `T015` `T016`  | 案件の置き場／顧客に案件をつくれる       | #19 #20 |
| `T017`         | 案件の進み具合を変えられる               | #21     |
| `T018`         | 案件を段階ごとに並べて見られる（/deals） | #22     |
| `T019` `T020`  | 顧客の画面に段階つきで並ぶ／金額と時期   | #23     |
| `T021`〜`T023` | 共通の枠とメニュー／スマホ幅／空の案内   | #24     |
| `T024`         | ごみ箱から元に戻せる                     | #25     |
| `T025`         | 表計算用のファイルに書き出せる           | #26     |
| `T026`         | 手元の顧客リストを取り込める             | #27     |
| `T031`         | 五十音順に並べる（＋公開の手順書）       | #28     |

- **20項目到達時の全体照合（`/plan-verify`）を実施し、壊れている項目は無かった。**
  照合役が `T001`〜`T020` のうち16項目を1つの置き場で上から順に通し、すべて「できている」
- **途中で見つけて直した不具合が2件**（どちらも同じ PR の中で修正済み）
  - 検索の SQL にだけ絞り込みが無く、ごみ箱に入れた顧客が検索結果には出ていた（`T024`）
  - 顧客が0件のときの画面に「取り込み」への入口が無かった（`T026`）
- **`docs/deploy-steps.md` を追加。** `T027`〜`T030` の手順を、打ち込む文字をそのまま
  並べた形で書いた。未確認のことは `[曖昧]` と明示してある
- `package.json` に `deploy` と `db:setup:remote` を追加（`T027` の `verify` が
  `pnpm run deploy` を実行する手順のため）
- `pnpm run check`（304テスト） / `pnpm run build` 通過

## 次にやること

**`docs/deploy-steps.md` に沿って `T027`（ネット上に置く）を進める。ユーザーの手元での
作業なので、AI は伴走のみ。** ユーザーには「手順を教えてほしい」と答えてもらっている。

止まりやすいのは次の2箇所で、どちらも `[曖昧]` のまま残っている。

- **Cloudflare の無料枠の実際の上限**（`T027` の前に画面で確認してもらう）
- **Zero Trust（`T028` の鍵かけ）の画面の位置と名前**。画面のスクリーンショットを
  見せてもらってから、どこを押すかを伝える形にしてある

**`wrangler.toml` の `database_id` は仮の値（`local-only-placeholder`）。`T027` の手順5で
本物に差し替える。** 差し替え忘れると公開先で置き場が見つからない。

**本物の顧客データを入れるのは `T028`（自分以外は開けないようにする）を終えてから。**
`docs/plan.json` の依存関係もその順になっている。**この順番を入れ替えないこと。**

`T029`（消えたときに戻せることを試す）は、書き出し→取り込みの往復が動くことは
`T025` `T026` の検証で確認済み。残っているのは「公開した画面で実際にやってみる」だけ。

## 注意点

**恒久的なリポジトリのルールはここに書かない。** `AGENTS.md`（指示）と `docs/decisions.md`
（根拠）を参照する。ここに書くのは、セッションをまたいで再発しうる作業上の落とし穴だけ。

- **五十音順はふりがな欄に頼っている。** 漢字だけでは読み方が決まらないため、ふりがなが
  空の顧客は五十音順の並びには入らず、名前順で後ろにまとまる。この設計はユーザーの
  判断（「ふりがなの欄を足す」）に基づく
- **`pgrep` / `kill` の対象を絞るときは、コマンド文字列に `wrangler` をそのまま書かない。**
  `pgrep -af "wrangl[e]r"` のように書く。**さらに、判定役（サブエージェント）が同時に
  動いているときは、自分のシェルのコマンド文字列に `wrangler` の文字を含めないこと。**
  判定役の後片付け（`kill`）が自分のシェルに当たって死ぬ（exit 144。今回2度踏んだ）。
  コミットメッセージに `wrangler.toml` と書いただけでも起きる。長い文章はファイルに
  書いて `git commit -F` で渡すと避けられる
- **`rm -rf` はこの環境で拒否される。** 置き場を空にしたいときは `.wrangler/` を
  scratchpad へ `mv` して退ける
- **`curl` はこの環境で拒否される。** HTTP の確認は `node -e` の `fetch` を使う
- **実ブラウザの実行ファイルは `/opt/pw-browsers/chromium`。** `playwright-core` は
  scratchpad 側に入れて使う。**localhost にプロキシを通さないよう、launch の args に
  `--proxy-bypass-list=<-loopback>` を付ける**
- **ファイルのダウンロードを確かめるときは `newContext({ acceptDownloads: true })` と
  `waitForEvent('download')`。** 画面に文字が出るだけでは「落ちてきた」ことにならない
- **1項目マージする前に次の項目を同じブランチへ push しない。** PR が2項目ぶんを含んで
  しまう（今回 `T019`＋`T020` で発生）。判定 → `done` → PR → マージ、まで終えてから次へ
- **作業ブランチに `main` を取り込むとき、squash マージ済みの同じ内容が衝突する。**
  自分のブランチ側（`--ours`）が常に上位互換なので、`git checkout --ours <files>` で
  解消してよい。差分は `git diff --stat origin/main HEAD` で確かめる
- **判定役への指示は短く区切らせる。**「id・判定・根拠2〜3行に絞る」と書くとよい
- **`verify` の手順は1つあたり8文字以上必要**（`src/plan.ts` の `validateVerifySteps`）
- **手元の SQLite（`node:sqlite`）は既定で外部キーを見ない。D1 は見る。** 表の検査を
  書くときは `PRAGMA foreign_keys = ON` を先に流す
- **テストの migration は名前を1つずつ書かず、`migrations/` を全部当てる。**
  書き足し忘れで「実物では動くのにテストだけ落ちる」が2度起きたため、全テストで
  ディレクトリを読む形に統一済み
- **`docs/plan.json` は一度確定したら本文を書き換えない。** できるのは `status` の変更と
  末尾への追記だけ。追記の番号は `pnpm run plan:next-id` で決める
- **リポジトリのコミット履歴が `f95fa9a Initial commit` にまとめ直されている。**
  2026-09-01 より前の引継ぎ文に出てくる PR 番号やコミット SHA はもう辿れない
- **この引継ぎ文の本文に、自動記録の目印（`session-end-stamp` の HTML コメント）を
  そのまま書かないこと。** 書くと自動記録がそこで本文を切り落とす

## セッション終了時点の状態（自動記録）

- 記録時刻: 2026-09-02 08:31 UTC
- ブランチ: `claude/checkin-d19vz8`
- HEAD: `984b220`
- 未コミットの変更:

```
M docs/handoff.md
```

<!-- session-end-stamp -->

## セッション終了時点の状態（自動記録）

- 記録時刻: 2026-09-02 13:01 UTC
- ブランチ: `claude/checkin-d19vz8`
- HEAD: `c57df0d`
- 未コミットの変更:

```
M docs/handoff.md
```
